#!/usr/bin/env bash
set -euo pipefail

REPO="${REPO:-$(cd "$(dirname "$0")/.." && pwd)}"

ln -sfn "$REPO/tmux/.tmux.conf" "$HOME/.tmux.conf"
ln -sfn "$REPO/tmux/.tmux.conf.local" "$HOME/.tmux.conf.local"
